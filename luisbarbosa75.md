entrega de projeto githubr
aula github.

apenas para entrega mesm
Utilize o gerenciador de pacotes pip para instalar o foobar.

pip install foobar
Uso
import foobar

# returns 'words'
foobar.pluralize('word')

# returns 'geese'
foobar.pluralize('goose')

# returns 'phenomenon'
foobar.singularize('phenomena')
Contribuindo
