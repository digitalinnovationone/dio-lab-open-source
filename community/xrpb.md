As configurações do utilizador  podem ser visualizadas través do comando abaixo:
git config --list

Configurando user-name:
git config --global user.name "nomedousuario"

configurando email:
git config --global user.email "seuemail"

Iniciando repositório:
mkdir git-project

Adc novo arquivo:

touch index.html
