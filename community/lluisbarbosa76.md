curso github aula teste arquivo2
arquivo de teste

Utilize o gerenciador de pacotes pip para instalar o foobar.

pip install foobar
Uso
import foobar

# returns 'words'
foobar.pluralize('word')

# returns 'geese'
foobar.pluralize('goose')

# returns 'phenomenon'
foobar.singularize('phenomena')
Contribuindo
